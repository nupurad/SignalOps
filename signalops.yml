services:
  demo-service:
    base_url: https://jsonplaceholder.typicode.com
    endpoints:
      - path: /posts
        method: GET
        interval: 1
      - path: /users
        method: GET
        interval: 1
      - path: /invalid-endpoint
        method: GET
        interval: 1
      
